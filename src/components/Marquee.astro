---
---

<div class="marquee" aria-hidden="true">
  <div class="track">
    <span>★ Welcome to Vertical Latency ★</span>
    <span class="hot">Web Developer since 2005</span>
    <span>★ Central Florida ★</span>
    <span class="hot">TypeScript · React · Next.js</span>
    <span>★ Thanks for stopping by ★</span>
  </div>
</div>

<style>
  .marquee {
    margin-top: 22px;
    overflow: hidden;
    white-space: nowrap;
    border-top: 2px solid var(--color-hr);
    border-bottom: 2px solid var(--color-hr);
    padding: 8px 0;
    background: rgba(0,0,0,.25);
  }
  .marquee .track {
    display: inline-block;
    padding-left: 100%;
    animation: scrollx 22s linear infinite;
  }
  .marquee .track span {
    font-family: var(--font-vt323);
    font-size: 21px;
    letter-spacing: 1px;
    color: var(--color-neon4);
    margin: 0 22px;
    text-shadow: 0 0 8px rgba(93,255,122,.5);
  }
  .marquee .track .hot {
    color: var(--color-neon2);
    text-shadow: 0 0 8px rgba(255,54,208,.5);
  }
</style>
